pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Build Stage from Jenkinsfile'
      }
    }
    stage('Test') {
      steps {
        echo 'Test Stage from Jenkinsfile'
      }
    }
    stage('Deploy') {
      steps {
        echo 'Deploy Stage from Jenkinsfile'
      }
    }
  }
}
